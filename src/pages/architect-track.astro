---
// src/pages/architect-track.astro
// Track 4 - Architect overview page (Coming Q3 2025)
import Layout from '../layouts/Layout.astro';
---

<Layout title="Track 4: Architect - AgenticControl.dev">
  <div class="min-h-screen bg-background">
    <!-- Header -->
    <header class="border-b border-neonPink/30 bg-surface/40 sticky top-0 z-50 backdrop-blur-sm">
      <div class="max-w-7xl mx-auto px-4 py-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between">
          <a href="/" class="text-sm font-mono text-slate-400 hover:text-neonCyan transition-colors">
            ← Back to Home
          </a>
          <div class="flex items-center gap-3">
            <div class="px-3 py-1 bg-neonPink/20 border border-neonPink rounded text-xs font-mono text-neonPink">
              🔴 TRACK 4
            </div>
            <span class="text-sm font-mono text-slate-400">
              Coming Q3 2025
            </span>
          </div>
        </div>
      </div>
    </header>

    <!-- Hero Section -->
    <section class="border-b border-neonPink/30 bg-gradient-to-b from-neonPink/5 to-background">
      <div class="max-w-7xl mx-auto px-4 py-16 sm:px-6 lg:px-8">
        <!-- Track Badge -->
        <div class="inline-block px-4 py-2 bg-neonPink/20 border border-neonPink rounded-lg font-mono text-sm text-neonPink mb-6">
          🔴 TRACK 4 - ARCHITECT
        </div>

        <!-- Title -->
        <h1 id="hero-title" class="font-display text-5xl md:text-7xl text-neonPink mb-6 tracking-wider leading-tight">
          <span class="animate-cursor-blink">█</span>
        </h1>

        <!-- Description -->
        <div class="max-w-3xl space-y-4 text-lg text-slate-300 mb-8">
          <p class="text-2xl text-slate-200 font-semibold">
            For senior engineers, technical founders, and platform architects designing distributed agentic systems.
          </p>
          <p>
            You've deployed production systems in Track 3. Now you need to <strong class="text-neonPink">ARCHITECT</strong> platforms that support hundreds of agents, cross-facility coordination, federated learning, and emergent intelligence.
          </p>
          <p>
            This is the <strong class="text-neonPink">most advanced track</strong>: distributed agent architectures, platform orchestration, meta-learning systems, and designing for emergence. Not tutorials—deep architectural patterns.
          </p>
        </div>

        <!-- Meta Information Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
          <!-- Total Tutorials -->
          <div class="bg-surface/60 border border-neonPink/40 rounded-lg p-4">
            <div class="text-xs font-mono text-slate-400 mb-1">TUTORIALS</div>
            <div class="text-2xl font-display text-neonPink">
              18 tutorials
            </div>
            <div class="text-xs text-slate-500 mt-1">Expert level</div>
          </div>

          <!-- Release Date -->
          <div class="bg-surface/60 border border-neonPink/40 rounded-lg p-4">
            <div class="text-xs font-mono text-slate-400 mb-1">RELEASE</div>
            <div class="text-2xl font-display text-neonPink">
              Q3 2025
            </div>
            <div class="text-xs text-slate-500 mt-1">July - September</div>
          </div>

          <!-- Progress -->
          <div class="bg-surface/60 border border-neonPink/40 rounded-lg p-4">
            <div class="text-xs font-mono text-slate-400 mb-1">STATUS</div>
            <div class="text-2xl font-display text-neonPink">
              Planning
            </div>
            <div class="text-xs text-slate-500 mt-1">Not started</div>
          </div>
        </div>

        <!-- What You'll Learn -->
        <div class="max-w-3xl bg-surface/40 border border-neonPink/40 rounded-xl p-6 mb-8">
          <h2 class="font-display text-2xl text-neonPink mb-4">CORE OUTCOMES</h2>
          <div class="grid md:grid-cols-2 gap-4 text-sm text-slate-300">
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Distributed agent architectures</strong> across facilities</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Platform orchestration</strong> for 100+ agents</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Meta-learning systems</strong> (agents that improve agents)</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Federated learning</strong> without sharing raw PLC data</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Emergent intelligence</strong> from multi-agent interactions</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Cross-facility coordination</strong> patterns</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Agent marketplace design</strong> (buy/sell agent capabilities)</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Self-improving systems</strong> with reinforcement learning</span>
            </div>
            <div class="flex items-start gap-2">
              <span class="text-neonPink mt-1">→</span>
              <span><strong>Platform economics</strong> & cost modeling at scale</span>
            </div>
          </div>
        </div>

        <!-- Prerequisites -->
        <div class="max-w-3xl bg-vhs/40 border border-slate-600/40 rounded-xl p-6">
          <h2 class="font-display text-xl text-slate-200 mb-4">PREREQUISITES</h2>
          <ul class="space-y-2 text-slate-300 text-sm">
            <li class="flex items-start gap-2">
              <span class="text-neonPink">→</span>
              <span><strong>Complete Tracks 1, 2, and 3</strong> - This is the final track</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-slate-500">•</span>
              <span>Production deployment experience with multi-agent systems</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-slate-500">•</span>
              <span>Distributed systems architecture knowledge</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-slate-500">•</span>
              <span>Advanced Python (async, concurrency, event loops)</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-slate-500">•</span>
              <span>Platform thinking (designing for others to build on)</span>
            </li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Coming Soon Section -->
    <section class="max-w-7xl mx-auto px-4 py-16 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center">
        <div class="bg-surface/40 border border-neonPink/40 rounded-xl p-12">
          <div class="text-6xl mb-6">🏗️</div>
          <h2 class="font-display text-3xl text-neonPink mb-4">COMING Q3 2025</h2>
          <p class="text-slate-300 mb-6">
            Track 4 represents the culmination of the entire learning path. Master Tracks 1-3 first.
          </p>
          <div class="space-y-4">
            <p class="text-sm font-mono text-slate-400">
              Architecture is earned through production experience.
            </p>
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 max-w-2xl mx-auto">
              <a
                href="/technician-track"
                class="px-4 py-3 bg-surface border-2 border-neonPink text-neonPink font-mono text-sm rounded-lg hover:bg-neonPink hover:text-background transition-all"
              >
                → TRACK 1
              </a>
              <a
                href="/developer-track"
                class="px-4 py-3 bg-surface border-2 border-neonPink text-neonPink font-mono text-sm rounded-lg hover:bg-neonPink hover:text-background transition-all"
              >
                → TRACK 2
              </a>
              <a
                href="/system-deployment-track"
                class="px-4 py-3 bg-surface border-2 border-neonPink text-neonPink font-mono text-sm rounded-lg hover:bg-neonPink hover:text-background transition-all"
              >
                → TRACK 3
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer Navigation -->
    <footer class="border-t border-neonPink/30 bg-surface/40">
      <div class="max-w-7xl mx-auto px-4 py-8 sm:px-6 lg:px-8">
        <div class="flex flex-col md:flex-row justify-between items-center gap-4">
          <a href="/" class="text-sm font-mono text-slate-400 hover:text-neonCyan transition-colors">
            ← Back to All Tracks
          </a>
          <div class="flex gap-6 text-sm font-mono text-slate-500">
            <span>Track 4 of 4</span>
            <span>•</span>
            <span>18 Tutorials</span>
            <span>•</span>
            <span>Coming Q3 2025</span>
          </div>
        </div>
      </div>
    </footer>
  </div>
</Layout>

<style>
  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }

  /* Custom scrollbar for track page */
  ::-webkit-scrollbar {
    width: 10px;
  }

  ::-webkit-scrollbar-track {
    background: #0d0d0f;
  }

  ::-webkit-scrollbar-thumb {
    background: #ff4fd8;
    border-radius: 5px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: #d93fb3;
  }
</style>

<script>
  // Typewriter effect for hero title (runs on every navigation with view transitions)
  let typewriterTimeouts = [];

  const clearTypewriter = () => {
    typewriterTimeouts.forEach(timeout => clearTimeout(timeout));
    typewriterTimeouts = [];
  };

  function initTypewriter() {
    const el = document.getElementById('hero-title');
    if (!el) return;

    clearTypewriter();

    const lines = [
      '"NOW I DESIGN',
      'PLATFORMS,',
      'NOT PROJECTS."'
    ];
    let lineIndex = 0;
    let charIndex = 0;
    let currentHtml = '';

    el.innerHTML = '<span class="animate-cursor-blink">█</span>';

    function typeWriter() {
      if (!el) return;
      if (lineIndex < lines.length) {
        if (charIndex < lines[lineIndex].length) {
          currentHtml += lines[lineIndex].charAt(charIndex);
          el.innerHTML = currentHtml + '<span class="animate-cursor-blink">█</span>';
          charIndex++;
          typewriterTimeouts.push(setTimeout(typeWriter, 50));
        } else {
          lineIndex++;
          charIndex = 0;
          if (lineIndex < lines.length) {
            currentHtml += '<br/>';
            typewriterTimeouts.push(setTimeout(typeWriter, 100));
          } else {
            typewriterTimeouts.push(setTimeout(() => {
              if (el) el.innerHTML = currentHtml;
            }, 1000));
          }
        }
      }
    }

    typewriterTimeouts.push(setTimeout(typeWriter, 300));
  }

  // Run immediately for this page view
  initTypewriter();

  // Run once after the Astro navigation completes (avoids stacking multiple listeners)
  document.addEventListener('astro:page-load', initTypewriter, { once: true });

  // Clear pending timers before the page is swapped out
  document.addEventListener('astro:before-swap', clearTypewriter, { once: true });

  // Fallback for BFCache restores
  window.addEventListener('pageshow', function(event) {
    if (event.persisted) {
      initTypewriter();
    }
  });
</script>
