---
// src/pages/developer-track.astro
// Track 2 - Developer overview page (Coming Q1 2025)
import Layout from '../layouts/Layout.astro';
---

<Layout title="Track 2: Developer - AgenticControl.dev">
  <div class="min-h-screen bg-background">
    <!-- Header -->
    <header class="border-b border-amber/30 bg-surface/40 sticky top-0 z-50 backdrop-blur-sm">
      <div class="max-w-7xl mx-auto px-4 py-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between">
          <a href="/" class="text-sm font-mono text-slate-400 hover:text-neonCyan transition-colors">
            ← Back to Home
          </a>
          <div class="flex items-center gap-3">
            <div class="px-3 py-1 bg-amber/20 border border-amber rounded text-xs font-mono text-amber">
              🟡 TRACK 2
            </div>
            <span class="text-sm font-mono text-slate-400">
              Coming Q1 2025
            </span>
          </div>
        </div>
      </div>
    </header>

    <!-- Hero Section -->
    <section class="border-b border-amber/30 bg-gradient-to-b from-amber/5 to-background">
      <div class="max-w-7xl mx-auto px-4 py-16 sm:px-6 lg:px-8">
        <!-- Track Badge -->
        <div class="inline-block px-4 py-2 bg-amber/20 border border-amber rounded-lg font-mono text-sm text-amber mb-6">
          🟡 TRACK 2 - DEVELOPER
        </div>

        <!-- Title -->
        <h1 id="hero-title" class="font-display text-5xl md:text-7xl text-amber mb-6 tracking-wider leading-tight">
          <span class="animate-cursor-blink">█</span>
        </h1>

        <!-- Description -->
        <div class="max-w-3xl space-y-4 text-lg text-slate-300 mb-8">
          <p class="text-2xl text-slate-200 font-semibold">
            For automation engineers and AI developers ready to build multi-agent coordination systems.
          </p>
          <p>
            Track 1 taught you to <strong class="text-amber">USE</strong> agentic AI safely. Track 2 teaches you to <strong class="text-amber">BUILD</strong> industrial-grade multi-agent systems with parallel validation, event-driven coordination, and fault-intelligent behavior.
          </p>
          <p>
            You'll learn LangGraph state machines, multi-agent debate patterns, parallel worker swarms, and how to orchestrate agents that validate each other's outputs before any PLC code touches hardware.
          </p>
        </div>

        <!-- Meta Information Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
          <!-- Total Tutorials -->
          <div class="bg-surface/60 border border-amber/40 rounded-lg p-4">
            <div class="text-xs font-mono text-slate-400 mb-1">TUTORIALS</div>
            <div class="text-2xl font-display text-amber">
              12 tutorials
            </div>
            <div class="text-xs text-slate-500 mt-1">Intermediate level</div>
          </div>

          <!-- Release Date -->
          <div class="bg-surface/60 border border-amber/40 rounded-lg p-4">
            <div class="text-xs font-mono text-slate-400 mb-1">RELEASE</div>
            <div class="text-2xl font-display text-amber">
              Q1 2025
            </div>
            <div class="text-xs text-slate-500 mt-1">January - March</div>
          </div>

          <!-- Progress -->
          <div class="bg-surface/60 border border-amber/40 rounded-lg p-4">
            <div class="text-xs font-mono text-slate-400 mb-1">STATUS</div>
            <div class="text-2xl font-display text-amber">
              In Development
            </div>
            <div class="text-xs text-slate-500 mt-1">0/12 published</div>
          </div>
        </div>

        <!-- What You'll Learn -->
        <div class="max-w-3xl bg-surface/40 border border-amber/40 rounded-xl p-6 mb-8">
          <h2 class="font-display text-2xl text-amber mb-4">CORE OUTCOMES</h2>
          <ul class="space-y-3 text-slate-300">
            <li class="flex items-start gap-3">
              <span class="text-amber mt-1">✓</span>
              <span><strong>Multi-agent coordination patterns</strong> (coordinator-worker, debate, hierarchical)</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="text-amber mt-1">✓</span>
              <span><strong>Parallel validation swarms</strong> where 3+ agents verify PLC code independently</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="text-amber mt-1">✓</span>
              <span><strong>Event-driven agent behavior</strong> (trigger on alarms, not polling loops)</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="text-amber mt-1">✓</span>
              <span><strong>LangGraph state machines</strong> for industrial agent workflows</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="text-amber mt-1">✓</span>
              <span><strong>Fault-intelligent systems</strong> that learn from PLC error patterns</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="text-amber mt-1">✓</span>
              <span><strong>Agent memory & context management</strong> for long-running industrial processes</span>
            </li>
          </ul>
        </div>

        <!-- Prerequisites -->
        <div class="max-w-3xl bg-vhs/40 border border-slate-600/40 rounded-xl p-6">
          <h2 class="font-display text-xl text-slate-200 mb-4">PREREQUISITES</h2>
          <ul class="space-y-2 text-slate-300 text-sm">
            <li class="flex items-start gap-2">
              <span class="text-amber">→</span>
              <span><strong>Complete Track 1 (Technician)</strong> - You must understand single-agent patterns first</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-slate-500">•</span>
              <span>Intermediate Python (classes, async/await, type hints)</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-slate-500">•</span>
              <span>PLC programming experience (ladder, ST, or SCL)</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-slate-500">•</span>
              <span>Basic understanding of REST APIs and webhooks</span>
            </li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Coming Soon Section -->
    <section class="max-w-7xl mx-auto px-4 py-16 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center">
        <div class="bg-surface/40 border border-amber/40 rounded-xl p-12">
          <div class="text-6xl mb-6">⚙️</div>
          <h2 class="font-display text-3xl text-amber mb-4">COMING Q1 2025</h2>
          <p class="text-slate-300 mb-6">
            Track 2 tutorials are currently in development. Subscribe to get notified when they're published.
          </p>
          <div class="space-y-4">
            <p class="text-sm font-mono text-slate-400">
              Want early access? Complete Track 1 first, then join the waitlist.
            </p>
            <a
              href="/technician-track"
              class="inline-block px-8 py-4 bg-amber/20 border-2 border-amber text-amber font-mono text-lg rounded-lg hover:bg-amber hover:text-background transition-all shadow-lg hover:shadow-[0_0_20px_rgba(254,194,11,0.6)]"
            >
              → START WITH TRACK 1
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer Navigation -->
    <footer class="border-t border-amber/30 bg-surface/40">
      <div class="max-w-7xl mx-auto px-4 py-8 sm:px-6 lg:px-8">
        <div class="flex flex-col md:flex-row justify-between items-center gap-4">
          <a href="/" class="text-sm font-mono text-slate-400 hover:text-neonCyan transition-colors">
            ← Back to All Tracks
          </a>
          <div class="flex gap-6 text-sm font-mono text-slate-500">
            <span>Track 2 of 4</span>
            <span>•</span>
            <span>12 Tutorials</span>
            <span>•</span>
            <span>Coming Q1 2025</span>
          </div>
        </div>
      </div>
    </footer>
  </div>
</Layout>

<style>
  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }

  /* Custom scrollbar for track page */
  ::-webkit-scrollbar {
    width: 10px;
  }

  ::-webkit-scrollbar-track {
    background: #0d0d0f;
  }

  ::-webkit-scrollbar-thumb {
    background: #fec20b;
    border-radius: 5px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: #d9a409;
  }
</style>

<script>
  // Typewriter effect for hero title (runs on every navigation with view transitions)
  let typewriterTimeouts = [];

  const clearTypewriter = () => {
    typewriterTimeouts.forEach(timeout => clearTimeout(timeout));
    typewriterTimeouts = [];
  };

  function initTypewriter() {
    const el = document.getElementById('hero-title');
    if (!el) return;

    clearTypewriter();

    const lines = [
      '"NOW I CAN BUILD',
      'SYSTEMS AND',
      'TRUST THEM."'
    ];
    let lineIndex = 0;
    let charIndex = 0;
    let currentHtml = '';

    el.innerHTML = '<span class="animate-cursor-blink">█</span>';

    function typeWriter() {
      if (!el) return;
      if (lineIndex < lines.length) {
        if (charIndex < lines[lineIndex].length) {
          currentHtml += lines[lineIndex].charAt(charIndex);
          el.innerHTML = currentHtml + '<span class="animate-cursor-blink">█</span>';
          charIndex++;
          typewriterTimeouts.push(setTimeout(typeWriter, 50));
        } else {
          lineIndex++;
          charIndex = 0;
          if (lineIndex < lines.length) {
            currentHtml += '<br/>';
            typewriterTimeouts.push(setTimeout(typeWriter, 100));
          } else {
            typewriterTimeouts.push(setTimeout(() => {
              if (el) el.innerHTML = currentHtml;
            }, 1000));
          }
        }
      }
    }

    typewriterTimeouts.push(setTimeout(typeWriter, 300));
  }

  // Run immediately for this page view
  initTypewriter();

  // Run once after the Astro navigation completes (avoids stacking multiple listeners)
  document.addEventListener('astro:page-load', initTypewriter, { once: true });

  // Clear pending timers before the page is swapped out
  document.addEventListener('astro:before-swap', clearTypewriter, { once: true });

  // Fallback for BFCache restores
  window.addEventListener('pageshow', function(event) {
    if (event.persisted) {
      initTypewriter();
    }
  });
</script>
