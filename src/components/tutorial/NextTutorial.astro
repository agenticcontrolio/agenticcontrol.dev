---
// NextTutorial.astro
// Next tutorial teaser section

interface Props {
  number: number;           // Next tutorial number
  title: string;            // Tutorial title
  description: string;      // Short description
  slug?: string;            // Optional direct link (defaults to nav system)
}

const { number, title, description, slug } = Astro.props;
---

<div class="mb-16 bg-gradient-to-r from-neonCyan/10 to-neonPurple/10 border-2 border-neonCyan/40 rounded-xl p-8">
  <h2 class="text-2xl font-display text-neonCyan mb-4 tracking-wider">
    🔜 NEXT TUTORIAL
  </h2>

  <p class="text-lg text-white font-semibold mb-3">
    Tutorial #{number} — {title}
  </p>

  <p class="text-base text-slate-300 leading-relaxed">
    {description}
  </p>

  {slug && (
    <div class="mt-6">
      <a
        href={`/tutorials/${slug}`}
        class="inline-block px-6 py-3 bg-neonCyan/10 border-2 border-neonCyan/60 text-neonCyan rounded-lg font-semibold hover:bg-neonCyan/20 hover:border-neonCyan transition"
      >
        Start Tutorial #{number} →
      </a>
    </div>
  )}
</div>
