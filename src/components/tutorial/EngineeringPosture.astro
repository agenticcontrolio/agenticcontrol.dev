---
// EngineeringPosture.astro
// Engineering posture/principles section for tutorials

interface Props {
  title?: string;           // Default: "ENGINEERING POSTURE"
  items: string[];          // Posture principles
}

const { title = "🧭 ENGINEERING POSTURE", items } = Astro.props;
---

<div class="bg-vhs/60 border-l-4 border-amber rounded-r-lg p-6">
  <h3 class="text-xl font-display text-amber mb-4 tracking-wide">
    {title}
  </h3>

  <p class="text-base text-slate-300 leading-relaxed mb-4">
    This tutorial enforced:
  </p>

  <ul class="space-y-2">
    {items.map((item) => (
      <li class="flex items-start gap-3 text-slate-300">
        <span class="text-amber flex-shrink-0">▸</span>
        <span>{item}</span>
      </li>
    ))}
  </ul>
</div>
